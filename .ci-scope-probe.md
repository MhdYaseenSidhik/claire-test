# CI scope probe

This file confirms the connected token can write to a normal path on this branch.
It isolates the `.github/workflows/` push failure to the missing `workflow` scope
rather than a general lack of write access. Safe to delete.
